import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Descomentar si se sirven imágenes de dominio externo:
  // images: {
  //   remotePatterns: [{ protocol: "https", hostname: "cdn.juaretires.com" }],
  // },
};

export default nextConfig;
